#include "glow.h"
#include <source-sdk/c_glow_object_manager.h>

void glow::glow() {
  // do stuff
}

namespace glow {
  // glowobjectmanager = reinterpret_cast<CGlowObjectManager*>(client_so_addr + 0x204F340);
}
